/**
 * @file
 * events_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.eventsTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
